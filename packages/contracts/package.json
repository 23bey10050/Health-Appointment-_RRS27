{
  "name": "@health/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Request and response shapes shared by the API and the browser, so the two cannot drift apart.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
